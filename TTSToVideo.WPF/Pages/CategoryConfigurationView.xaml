<Window x:Class="TTSToVideo.WPF.Pages.CategoryConfigurationView"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:local="clr-namespace:TTSToVideo.WPF.Pages"
     xmlns:viewsmodels="clr-namespace:TTSToVideo.WPF.ViewsModels"
     mc:Ignorable="d"
     Title="CategoryConfigurationView" Height="450" Width="800"
     d:DataContext="{d:DesignInstance Type=viewsmodels:NewCategoryViewModel}">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
    </Window.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding WindowClosedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" ></RowDefinition>
            <RowDefinition Height="auto" ></RowDefinition>
            <RowDefinition Height="70*" ></RowDefinition>
            <RowDefinition Height="30*" ></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel>
            <Label>Category</Label>
            <TextBox Text="{Binding Model.CategoryName, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <Label Grid.Row="1" >IA Chat Instructions</Label>
        <TextBox Grid.Row="2" Text="{Binding Model.IAChatInstructions}" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" 
                   HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                   Width="Auto" Height="Auto" />

        <StackPanel Grid.Row="3">
            <Label>Hash Tags</Label>
            <TextBox Text="{Binding Model.HashTags}" Height="50" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
        </StackPanel>

    </Grid>
</Window>
